import { Schema } from "@tiptap/pm/model";
export declare const parsePreCode: (el: HTMLElement) => {
    language: string | undefined;
} | undefined;
export declare const parsePreCodeContent: ({ el, schema, }: {
    el: HTMLElement;
    schema: Schema;
}, blockType: string) => import("prosemirror-model").Fragment;
